program gcd;

type 
	Bp = boolean;
	IP = integer;

var a,b,c:integer;
	inpt: string;
	d: Bp;
	e: boolean;
	f,h: real;
	k:HAHA;

inpt:string;

{
	function to find greatest common divisor
}



function GCD(m,n:integer):integer;
	begin
		while m<>n do 
		if m>n then m:=m-n else n:=n-m;
		GCD:=m;
	end;


var 
	v1:real;
	v2: IP;

function t():real;
begin
	t := 13;
end;

begin
	inpt := 'Input 3 numbers';
	writeln(inpt);

	e:= true;
	d := e;

	f := 123.4567;

	t:= ' 14214214214';

	e := 12 + -13*14/(35-14.33)+GCD(3,5,4)/t;

	readln(inpt);

	c := GCD(GCD(a,b),c);
	writeln('The gcd of 3 numbers is:');
	
	a := (a+b)*c - d/ ( 312 + 40);
	if ---12 + 13 >= ((4*5)+10 > 5) - 'lol' then
	begin
		writeln('Fine');
	end;


	vr := 'string + int' + 20;

end.


